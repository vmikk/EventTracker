<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homePagerFragment">

    <fragment
        android:id="@+id/homePagerFragment"
        android:name="dev.vmikk.eventtracker.HomePagerFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home_pager">
    </fragment>

    <fragment
        android:id="@+id/calendarFragment"
        android:name="dev.vmikk.eventtracker.CalendarFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_calendar">
    </fragment>

    <fragment
        android:id="@+id/settingsFragment"
        android:name="dev.vmikk.eventtracker.SettingsFragment"
        android:label="@string/action_settings"
        tools:layout="@layout/fragment_settings">
    </fragment>
</navigation>